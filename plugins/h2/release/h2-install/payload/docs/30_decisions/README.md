# 30_decisions

프로젝트 결정 기록을 보관한다.

## 사용 기준

- 되돌아볼 가치가 있는 architecture, product, workflow 결정을 남긴다.
- decision 문서는 파일명 suffix와 frontmatter `status`가 일치해야 한다.
- rejected decision은 반복 제안을 줄이기 위해 회피 문구를 함께 남긴다.

## 파일 패턴

```text
docs/30_decisions/{area}/NNNN-{topic}.{status}.md
```

